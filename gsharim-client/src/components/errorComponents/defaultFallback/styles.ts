import { SystemStyleObject } from "@mui/system";
import { CSSProperties } from "react";

export const fallbackContainer: SystemStyleObject = {
	display: "flex",
	flexDirection: "column",
	alignItems: "center",
	justifyContent: "center",
};

export const lottie: CSSProperties = {
	height: "300px",
};
