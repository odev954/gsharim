// this file is being prefetched beacuse it is big and takes time do fetch, so we prefetch it early on to be cached

fetch("/pyodide/pyodide.asm.wasm");
fetch("/pyodide/pyodide.asm.data");
export {};
